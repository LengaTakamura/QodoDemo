      - name: PR Agent action step
        uses: qodo-ai/pr-agent@v0.27
        env:
          OPENAI_KEY: ${{ secrets.OPENAI_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # ==== モデル設定 ====
          CONFIG.MODEL: "gpt-5.1-codex-mini"
          CONFIG.FALLBACK_MODELS: '["gpt-5.1-codex-mini"]'

          # ==== reasoning 設定 ====
          CONFIG.REASONING_EFFORT: "low"
          PR_DESCRIPTION.REASONING_EFFORT: "low"
          PR_REVIEWER.REASONING_EFFORT: "low"
          PR_CODE_SUGGESTIONS.REASONING_EFFORT: "low"

          CONFIG.USE_WIKI_SETTINGS_FILE: "false"
          CONFIG.USE_REPO_SETTINGS_FILE: "false"
          CONFIG.USE_GLOBAL_SETTINGS_FILE: "false"
          CONFIG.CUSTOM_REASONING_MODEL: "false"

          # ==== 日本語設定 ====
          CONFIG.RESPONSE_LANGUAGE: "ja-JP"
          PR_DESCRIPTION.EXTRA_INSTRUCTIONS: "すべて日本語で説明してください。英語は使用しないでください。"
          PR_REVIEWER.EXTRA_INSTRUCTIONS: "すべて日本語でレビューしてください。英語は使用しないでください。"
          PR_CODE_SUGGESTIONS.EXTRA_INSTRUCTIONS: "すべて日本語で改善提案をしてください。英語は使用しないでください。"

          PR_REVIEWER.REQUIRE_SCORE_REVIEW: true
          PR_REVIEWER.REQUIRE_CAN_BE_SPLIT_REVIEW: true
          PR_CODE_SUGGESTIONS.FOCUS_ONLY_ON_PROBLEMS: false
          PR_CODE_SUGGESTIONS.ALLOW_THUMBS_UP_DOWN: true
